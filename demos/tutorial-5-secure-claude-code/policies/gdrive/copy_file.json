{
  "policy_id": "mcp:claude_ai_Google_Drive/copy_file",
  "version": "2.5.0",
  "scope": "mcp",
  "extends": "app:claude_ai_Google_Drive",
  "resources": [
    "mcp:claude_ai_Google_Drive/copy_file"
  ],
  "attestations": [
    "drive_write_approved"
  ],
  "description": "Drive copy_file via connector; duplicating into a chosen parentId is ungateable, so approval gates the write."
}
